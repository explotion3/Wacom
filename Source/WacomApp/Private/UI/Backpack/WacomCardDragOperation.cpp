// Copyright Wacom. All Rights Reserved.

#include "UI/Backpack/WacomCardDragOperation.h"

// 字段全部使用 UPROPERTY 默认初始化（FGuid() / EZoneKind::Backpack / nullptr）。
// 当前没有需要在 cpp 中实现的成员函数；保留 cpp 文件用于将来扩展（如 OnDragged 视觉反馈）。
